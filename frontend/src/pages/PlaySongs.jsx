
const PlaySongs = () => {
  return (
    <div>
        <button className="playback">Back</button>
        <button className="playback">Play/Pause</button>
        <button className="playback">Forward</button>
    </div>
  );
}

export default PlaySongs;